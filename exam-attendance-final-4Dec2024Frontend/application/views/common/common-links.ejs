<link rel="stylesheet" href="/fontawesome-free-6.6.0-web/css/all.min.css" />

<link rel="stylesheet" href="/fontawesome-free-6.6.0-web/css/all.min.css" />

<!-- <link rel="stylesheet" href="/assets/css/common/common.css" /> -->

<link rel="stylesheet" href="/assets/css/plugins/toastify/toastify.min.css" />

<style>
    #btn-loader-container {
        display: none;

        position: absolute;
        width: 100%;
        height: 100%;
        left: 0;
        top: 0;

        & .btn-loader-inside {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;
            background-color: #4f46e5;
        }
    }

    #btn-loader {
        width: 35px;
        padding: 8px;
        aspect-ratio: 1;
        border-radius: 50%;
        /* background: #25b09b; */
        background-color: white;
        --_m: conic-gradient(#0000 10%, #000), linear-gradient(#000 0 0) content-box;
        -webkit-mask: var(--_m);
        mask: var(--_m);
        -webkit-mask-composite: source-out;
        mask-composite: subtract;
        animation: l3 1s infinite linear;
    }

    /* BELOW CSS IS FOR THE  lOADER CONTAINER*/

    #initial-text {
        text-align: center;
        padding-top: 5rem;
        font-size: 3rem;
        color: var(--text-color) !important;
        font-style: italic;
        font-weight: bold;
    }

    #loader-container {
        display: none;
        margin-top: 3rem;
        text-align: center;
        font-size: 1rem;
    }
    .ip {
        width: 16em;
        height: 8em;
    }
    .ip__track {
        stroke: hsl(var(--hue), 90%, 90%);
        transition: stroke var(--trans-dur);
    }
    .ip__worm1,
    .ip__worm2 {
        animation: worm1 2s linear infinite;
    }
    .ip__worm2 {
        animation-name: worm2;
    }

    /* Dark theme */
    @media (prefers-color-scheme: dark) {
        :root {
            --bg: hsl(var(--hue), 90%, 5%);
            --fg: hsl(var(--hue), 90%, 95%);
        }
        .ip__track {
            stroke: hsl(var(--hue), 90%, 15%);
        }
    }

    /* Animation */
    @keyframes worm1 {
        from {
            stroke-dashoffset: 0;
        }
        50% {
            animation-timing-function: steps(1);
            stroke-dashoffset: -358;
        }
        50.01% {
            animation-timing-function: linear;
            stroke-dashoffset: 358;
        }
        to {
            stroke-dashoffset: 0;
        }
    }

    @keyframes worm2 {
        from {
            stroke-dashoffset: 358;
        }
        50% {
            stroke-dashoffset: 0;
        }
        to {
            stroke-dashoffset: -358;
        }
    }

    & #btn-loader-container {
        display: none;

        position: absolute;
        width: 100%;
        height: 100%;
        left: 0;
        top: 0;

        & .btn-loader-inside {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;
            /* background-color: #0ea5e9; */
        }
    }

    #btn-loader {
        width: 28px;
        padding: 8px;
        aspect-ratio: 1;
        border-radius: 50%;
        /* background: #25b09b; */
        background-color: white;
        --_m: conic-gradient(#0000 10%, #000), linear-gradient(#000 0 0) content-box;
        -webkit-mask: var(--_m);
        mask: var(--_m);
        -webkit-mask-composite: source-out;
        mask-composite: subtract;
        animation: l3 1s infinite linear;
    }

    @keyframes l3 {
        to {
            transform: rotate(1turn);
        }
    }

    /* For INITIAL TEXT */
    #initial-text {
        text-align: center;
        padding-top: 5rem;
        font-size: 3rem;
        color: var(--text-color) !important;
        font-style: italic;
        font-weight: bold;
        /* display: none; */
    }
</style>
